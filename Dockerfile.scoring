# Scoring Service
FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY backend/scoring/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy code
COPY backend/scoring/ .

# Create directories
RUN mkdir -p data/scores

# Run
CMD python scoring_consumer.py
